# Optional: If you use API key auth instead of OAuth (claude login)
# ANTHROPIC_API_KEY=sk-ant-...

# Optional: Override Claude Code binary path
# CLAUDE_EXECUTABLE_PATH=/usr/local/bin/claude

# Optional: Log level (trace, debug, info, warn, error)
# LOG_LEVEL=info
